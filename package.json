{
  "dependencies": {
    "esbuild": "^0.14.36",
    "kaboom": "^2000.2.9"
  },
  "scripts": {
    "start": "./node_modules/.bin/esbuild main.js --bundle > game.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  }
}
